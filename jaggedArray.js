

/* 

JAGGED ARRAY 
-------------------------------------------------------------
jaggedArray is an array of arrays, 
and each inner array can have a different number of elements. 
The lengths of the inner arrays are not uniform.
-------------------------------------------------------------

*/


const jaggedArray = [
    [1, 2, 3, 4],
    [4, 3, 2, 1],
    [5, 6, 7],
    [7, 8]
]
console.log(jaggedArray);
console.log(jaggedArray[3]);
console.log(jaggedArray[1]);
console.log(jaggedArray.length); 